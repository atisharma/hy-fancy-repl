### TODO

* write tests

### Potential Future Features

* Bracket matching highlight - show which paren/bracket the cursor is on
* Better validation indicator - show *what* is unbalanced (e.g., "3 unclosed parens")
* Fuzzy completion - current completer is prefix-only
* REPL commands - `:help`, `:doc symbol`, `:paste` mode
* Output paging for large outputs
* Session save/restore - save variables between sessions
